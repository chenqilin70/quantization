<?xml version="1.0" encoding="UTF-8"?>
<sqls>
    <test>
        <![CDATA[
            select
                corr(i.close,n.LJJZ) as correlationindex,substring_index(n.rowkey,"_",1)
            from
                index i
                inner join netval n on substr(i.rowkey,instr(i.rowkey,"_")+1)=substr(n.rowkey,instr(n.rowkey,"_")+1)
                inner join fund f on f.jjlx='股票型'
            where
                i.close is not null
                and n.LJJZ is not null
            group by
                substring_index(n.rowkey,"_",1)
            order by
                correlationindex
        ]]>
    </test>
    <test2>
        <![CDATA[


            select
                substr(n.rowkey,instr(n.rowkey,"_")+1)
            from
                netval n


                where
                substring_index(i.rowkey,"_",1)='SH000300'
                and substring_index(n.rowkey,"_",1)='161604'




        ]]>
    </test2>
    <index>
        <![CDATA[
            select * from index
        ]]>
    </index>

</sqls>